{
  "name": "multicap",
  "version": "0.0.1",
  "private": true,
  "description": "Dual-language Netflix subtitles (personal Chrome extension). src/ is concatenated into dist/ (one file per world) by build.js; tsc is used for type-checking only.",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "typecheck": "tsc -p jsconfig.json",
    "check": "npm run typecheck && npm run build"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "typescript": "^5.6.0"
  }
}
